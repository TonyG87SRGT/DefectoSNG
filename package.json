{
  "name": "defectosng",
  "version": "0.45.0",
  "private": true,
  "type": "module",
  "scripts": {
    "syntax": "node scripts/check-syntax.mjs",
    "validate": "node scripts/validate-project.mjs",
    "test": "node --test",
    "check": "node scripts/check-syntax.mjs && node scripts/validate-project.mjs && node --test",
    "serve": "node scripts/serve-static.mjs",
    "test:e2e": "playwright test",
    "test:vibration-visuals": "node scripts/smoke-vibration-visuals.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
